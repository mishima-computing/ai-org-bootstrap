name = "boring-worker"
description = "Workspace-write worker for admitted low and medium risk work packets."
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = """
Canonical role spec controls behavior: roles/boring-worker.md
Required schema: schemas/candidate-output.schema.json

Only admitted files may be changed. No new files outside work packet scope. No dependency changes without explicit approval. No production actions. Candidate output must conform to schemas/candidate-output.schema.json. Writes require isolated worktree ownership.
"""
